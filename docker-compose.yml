services:
  redis:
    image: redis:7-alpine
    container_name: prep_pilot_redis
    command: ['redis-server', '--appendonly', 'yes']
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data 

volumes:
  redis_data: